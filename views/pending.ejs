<%- include("partials/headers") -%>
<div class="box" id="heading">
    <h1> Pending tasks </h1>
    <a href="/">Home</a>
  </div>

<div class="box">
<% Tasks.forEach(function(Task){ %>
        <div class="item">
        <form action="/pending?_method=delete" method="post">
          <input type="Checkbox" name="checkbox" value="<%= Task._id %>" onChange="this.form.submit()">
        </form>
        <form class="form" action="/pending?_method=PATCH" method="POST">
          <p> <%= Task.item %> </p>
            <input type="Checkbox"  id="check" name="id" value="<%= Task._id %>" onChange="this.form.submit()">
          </form>
        </div>
<% }); %>
</div>
<%- include("partials/footer") -%>